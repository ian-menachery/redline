# Spike-phase dependencies (Phase 0.5).
# Graduate to pyproject.toml at Phase 1 start.

# Install the local redline package itself (src-layout). Streamlit Cloud
# runs `pip install -r requirements.txt` and would otherwise fail at
# `from redline.config import RedlineConfig` because the src/ layout
# means `redline` isn't importable until pip-installed.
-e .

edgartools>=3.0
pydantic>=2.0
pyyaml>=6.0
anthropic>=0.40
python-dotenv>=1.0
pytest>=8.0
streamlit>=1.30

# OpenAI client — Phase 1 starts on OpenAI (gpt-4o-mini + gpt-4o) to consume
# Ian's $4.98 of OpenAI credits, then falls over to Anthropic on
# insufficient_quota. See ARCHITECTURE.md §9 (Provider Fallover).
openai>=1.0

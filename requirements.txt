# Spike-phase dependencies (Phase 0.5).
# Graduate to pyproject.toml at Phase 1 start.

edgartools>=3.0
pydantic>=2.0
pyyaml>=6.0
anthropic>=0.40
python-dotenv>=1.0

# Spike-only: OpenAI client for the Phase 0.5 Stage-2 dry-run.
# Remove before Phase 1 starts (Phase 1 production uses Anthropic per ARCHITECTURE.md §9).
openai>=1.0
